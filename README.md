- Установка зависимостей (нужен uv):
  `uv sync`

- Запуск:
  `uvicorn main:app --reload`

- Тесты(e2e):
  `uv run pytest`

- Конфигурация:
  В данном приложении можно конфигурировать начальные условия генерации гексагонов (центр и радиус окружности) через .env
